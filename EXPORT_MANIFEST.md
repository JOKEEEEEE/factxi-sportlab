# Archive SportLab v0.1 — FACT XI

Cette archive contient l'état du projet au 5 août 2026.

## Interface

- `index.html` : accueil et sélection compétition/saison/club/match.
- `match.html` : MatchLab Arsenal–Manchester City.
- `home.css`, `home.js` : accueil.
- `matchlab.css`, `matchlab.js` : analyse du match.
- `editorial.css`, `product.css`, `revision.css`, `logos.css` : styles complémentaires.
- `styles.css`, `app.js`, `match.css`, `analysis.css` : premières maquettes conservées comme archives.

## Données et architecture

- `src/sportlab/` : modèles, configuration et fournisseurs.
- `editorial-schema.json` : contrat fixe du carrousel X.
- `.env.example` : variables d'environnement attendues.
- `pyproject.toml` : définition du paquet Python.

## Tests et documentation

- `tests/` : cinq tests hors ligne.
- `README.md` : documentation de démarrage.
- `docs/v0.1-status.md` : état et limites de la v0.1.
- `AGENTS.md` : contexte et règles du projet FACT XI.

## Références visuelles

- `Logo Transparent.png` : logo FACT XI fourni.
- `codex-clipboard-4268c294-a230-478e-b173-3217ed3c2402.png` : référence visuelle Tesla fournie.

Les dossiers `__pycache__` et les secrets locaux ne sont pas inclus.
